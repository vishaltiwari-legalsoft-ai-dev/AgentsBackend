__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example
gcp-service-account.json
*.pem
*.log
.pytest_cache/
# Cloud Run deploy env (contains secrets); keep only the .example
deploy.env.yaml
# Graphics Designer generated run artifacts (output, not source)
agents/**/runs/
agents/**/creative_runs/
# npm scratch installs used to vendor static assets (not project deps)
node_modules/

# dynamically materialized brand fonts (downloaded from GCS at runtime; static brand fonts stay committed)
agents/Graphics designer agent/graphics_designer_agent/brands/ai-answering/
