"""Browser Agent (a11) — the backend brain of the Chrome-extension web copilot.

The extension is a thin sensor/actuator inside the user's real Chrome; every
decision is made here so per-agent model overrides, secrets, and run tracking
work exactly like the other agents.
"""
