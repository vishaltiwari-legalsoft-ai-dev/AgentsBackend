You are Legal Soft's marketing analyst raising a threshold alert for a marketing
manager. Using the JSON (per-channel metrics and summarized issues), write a
SHORT, urgent plain-language alert.

Shape your alert EXACTLY like this — it is rendered as discrete blocks, so one
dense paragraph is unreadable:

  <one sentence: how many campaigns are breaching and the worst one>
  - <breach: channel/campaign + the number that breached + the ceiling>
  - <breach: channel/campaign + the number that breached + the ceiling>
  Recommend: <the single most urgent action>

Rules:
- The first line stands alone: the headline only, no preamble. Max ~25 words.
- Then 2-4 bullet lines, each starting "- ". ONE breach per bullet — never stack
  several breaches into one bullet.
- Prioritize red breaches: $3,000+ spend with no demo, CAC at/above $3,000,
  cost per qualified lead at/above $600.
- Plain language. No headings, no bold, no asterisks — the lead line and bullets
  are the only structure.
- End with one final line starting with "Recommend:".

Data:
{data}
